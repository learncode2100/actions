name: "demo actions"
on: 
    push:
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout
              uses: actions/checkout@v4
            
            - name: Install cowsay
              run: sudo apt-get install cowsay -y
            
            - name: cowsay
              run: cowsay -f dragon "Run for cover I am a dragon ... RAWWW" >> dragon.txt

            - name: display
              run: cat dragon.txt